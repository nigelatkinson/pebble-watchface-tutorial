#pragma once

#include <pebble.h>

/**
 * Update the time string displayed on the TextLayer
 */
void update_timeish(TextLayer *t, TextLayer *s);

/**
 * Update the time string displayed on the TextLayer
 */
void update_time(TextLayer *t);

/**
 * Update the time string displayed on the TextLayer
 */
void update_date(TextLayer *t);